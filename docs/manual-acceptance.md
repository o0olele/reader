# 手测清单 · F1 / F2 / F3（GUI 验收）

> **用途**：`ROADMAP-v3.md` 里 §6.1 #7（逐页对位）、#8（九组快捷键）、F1/F2/F3 的验收条目
> 依赖真实桌面会话，本机（无 GUI）做不了，因此单独立成文档。
> **代码基线**：`474a8bd`。执行前先跑一遍自动门禁，确认基线干净：
> `npm run lint && npm run format:check && npm run build`、
> `cargo clippy --manifest-path src-tauri/Cargo.toml --target-dir src-tauri/.cargo-target --all-targets -- -D warnings`、
> `cargo test --manifest-path src-tauri/Cargo.toml --target-dir src-tauri/.cargo-target`。
> 门禁红的时候不要开始手测 —— 那是「打勾但不为真」的第二次重演。

---

## 0. 准备

### 0.1 启动

```powershell
npm run tauri dev                      # 开发态（需要 WebView2 + Rust 工具链）
# 或
npm run tauri build                    # 产物：src-tauri/target/release/reader-desktop.exe
```

### 0.2 数据准备

| # | 准备项 | 为什么 |
| --- | --- | --- |
| D1 | 一本**本地 TXT**，单章 ≥ 100 万字 | C13 性能项 |
| D2 | 至少 3 本书 + 2 个分组 | B-bookshelf 的侧栏/多选/右键 |
| D3 | 至少 1 个启用「发现」的在线书源 + 可用网络 | B-explore |
| D4 | 一本**真实在线书**（章节数别太多，50 章以内） | D2「读完一本在线书」 |
| D5 | 一本**有「第X卷 / 部 / 篇」标题前缀**的书 | C7 卷分组 |
| D6 | 一本**含对话引号**的书 | C8 对话行 |

生成 D1：

```powershell
$body = ('雪落在长街上，没有声音。' * 100000)
Set-Content -Path "$env:USERPROFILE\Desktop\百万字单章.txt" -Encoding UTF8 `
  -Value ("第一章 长夜`n`n" + $body)
```

### 0.3 记录方式

- 每项填 `实测` 与 `结论`（✅ / ❌ / ⚠️ 偏差）。
- 截图存 `docs/qa/<YYYYMMDD>/<编号>.png`，例如 `docs/qa/20260910/C1.png`。
- 失败项按 §G 模板登记，**不要**直接改 `ROADMAP-v3.md` 的勾。
- 全部通过后按 §H 回填。

---

## A. F1 · 九组快捷键（§6.1 #8）

对照原型 `desktop-ui.html:2757–2776`。**每组都要额外验证「焦点在输入框内时不触发」**（原型 :2759 只跳过 input/textarea，本实现还跳过 select 与 contenteditable）。

| # | 按键 | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- | --- |
| A1 | `Esc` | ① `Ctrl+K` 打开面板后按 `Esc` ② 在首页无弹层时按 `Esc` | ① 面板关闭 ② 无副作用（不导航、不改主题） | | |
| A2 | `Ctrl+B` | 在首页按一次、再按一次；然后**重启应用** | rail 在 **220px ↔ 56px** 间切换；重启后保持上次状态（`localStorage['rail-extended']`） | | |
| A3 | `Ctrl+K` | 打开面板 → 输入一本书架里的书名 → 回车 | 出结果；回车跳到书架或阅读器 | | |
| A4 | `D` | 在首页按 `D`；再在书架搜索框内按 `D` | 主题浅/深切换；**输入框内按 `D` 只输入字符 d，不切主题** | | |
| A5 | `1`–`5` | 依次按 1…5 | 首页 / 书架 / 发现 / RSS / 我的（与原型 :2768 一致）；输入框内按数字**不导航** | | |
| A6 | `→` / `Space` | 阅读器**分页模式**下按；再切到滚动模式按 | 分页模式翻下一页；滚动模式与「单栏退回态」**不应拦截**（否则页面无法正常滚动） | | |
| A7 | `←` | 同上 | 上一页 | | |
| A8 | `T` | 阅读器内按 | 目录侧栏开关；输入框内按 `T` 不触发 | | |
| A9 | `F` | 阅读器内按 | 沉浸模式开关（见 C9） | | |

> A2 的 `Ctrl+B` 由 `src/app/useRail.ts` 提供，A1/A3/A4/A5 由 `src/app/useGlobalKeys.ts` 提供，
> A6–A9 由 `src/features/reader/ReaderPage.vue` + `useReaderPaging.ts` 提供。若某组失效，先按这个分工定位。

---

## B. F2 · 七页与原型并排比对（§6.1 #7）

逐页打开 `desktop-ui.html` 与运行中的应用**并排截图**。下表「原型」列是 `desktop-ui.html` 的行号区间。

| 页面 | 原型 | 必须对上的结构 | 允许的已知偏差 |
| --- | --- | --- | --- |
| home | `:1447–1619` | 双栏 `1fr / 336px`；hero 最近在读 + 3 格统计 + 每日目标环；侧栏 WebDAV 卡 | 统计取自真实 `get_reading_stats`；WebDAV 为**未接入态**（S）；原型的「快捷入口」组未实现 |
| bookshelf | `:1620–1687` | 左分组侧栏；工具条 搜索/排序/筛选/刷新/网格·列表切换；书卡网格/列表；多选浮动条；右键菜单 | 右键菜单 **7 项**（原型 9 项，缺「详情」「换源」，等 E1）；无「更新/完结」徽标（等 E1 书籍状态字段） |
| explore | `:1688–1835` | 左书源列表（状态圆点）+ 分类 chip + 书卡列表 | 原型**右栏**的 sticky 书源栏改到了左栏，分类 chip 改为右栏顶部条 |
| rss | `:1836–1944` | 三栏 `220 / 300 / 1fr` | **整页未接入态**（S）—— 这是纪律 F0 要求，不是缺页 |
| read | `:1945–2173` | 见 §C | 听书/AI/翻译/文本处理为未接入态 |
| my | `:2174–2290` | Web 服务卡 + 规则入口组 + 其他入口组 | Web 服务 / AI 对话 / 听书翻译为未接入态 |
| settings | `:2291–` | 左导航 + 右 pane | 实现 10 个 pane（原型 9 个，多出「封面」）；封面 / AI / 翻译 / 实验室为未接入态 |

### B1 · 壳层（与原型 rail/titlebar 对齐）

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| B1.1 | 观察 rail 展开态宽度 | 220px（`--rail-w-ext`） | | |
| B1.2 | `Ctrl+B` 后观察 | 56px（`--rail-w`）；分组标签「浏览 / 检索 / 阅读」保留；书架角标显示真实本数 | | |
| B1.3 | 观察 titlebar | 高度 40px（`--titlebar-h`）；品牌 + 面包屑 + 主题按钮 + 最小化/最大化/关闭 | | |
| B1.4 | 拖拽 titlebar 空白处 / 双击 | 窗口跟随拖动 / 双击最大化 | | |
| B1.5 | 点关闭按钮 | 应用退出（不是最小化到托盘 —— 托盘未接入） | | |
| B1.6 | 点 rail 底部「我的 / 设置」 | 路由正确；面包屑同步 | | |

### B2 · 书架交互细节

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| B2.1 | 书卡 hover | 右上角出现 下载 / 导出 TXT / 多选 三个按钮 | | |
| B2.2 | 右键一张书卡 | 7 项菜单：开始阅读 / 刷新目录 / 加入下载队列 / 导出 TXT / 导出 EPUB / 多选 / 删除书籍 | | |
| B2.3 | 勾选 2 本书 | 底部浮动条出现，显示「已选 2 本」+ 批量下载 / 批量删除 / 取消选择 | | |
| B2.4 | 切换网格 / 列表 | 两种布局都正常，列表态封面变小 | | |
| B2.5 | 搜索 + 排序 + 筛选组合 | 结果与筛选条件一致；无结果时显示空态 | | |

---

## C. F3 · 阅读器运行时几何（本次新写，最需要人眼）

### C0 · 触发条件先算清楚

双栏只在**正文内容宽 ≥ 760px 且 `window.innerHeight` ≥ 820px** 时出现
（`useReaderPaging.syncPageMetrics`，对应原型 `:870–885`）。
正文内容宽 = `窗口宽 − rail − 目录(288) − 面板(320) − 2×页边距(默认 32)`。

| 窗口宽 | rail | 目录 | 面板 | 正文内容宽 | 预期 |
| ---: | --- | --- | --- | ---: | --- |
| 1280 | 展开 220 | 关 | 关 | 996 | **双栏** |
| 1280 | 展开 220 | 开 | 关 | 708 | 退回单栏 |
| 1280 | 收起 56 | 关 | 关 | 1160 | **双栏** |
| 1500 | 展开 220 | 开 | 关 | 928 | **双栏** |
| 1500 | 展开 220 | 开 | 开 | 608 | 退回单栏 |
| 1700 | 展开 220 | 开 | 开 | 808 | **双栏** |

> 高度门槛量的是 `window.innerHeight`。若窗口带系统边框，**窗口高度需 ≈ 860px 以上**才稳过 820。

### C1 · 双栏落版

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C1.1 | 按上表挑一组「双栏」条件，打开一本长章节的书，切到**分页** | 正文两栏并排，中间 1px 竖分隔线，栏间距 64px | | |
| C1.2 | 观察 pager | 显示 `第 1 / N 页 · 本章 0%`，N ≥ 2 | | |
| C1.3 | 观察首页眉与标题 | 章节上方有卷名 eyebrow（无前缀则显示「正文」），标题在正文流内 | | |
| C1.4 | 观察首段 | 首字下沉（约 2.6em 大字） | | |

### C2 · 翻页与页码

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C2.1 | 点 pager 下一页 | 页码 +1，「本章 %」增长；到末页时按钮禁用 | | |
| C2.2 | 按 `→` / `Space` | 与点按钮一致 | | |
| C2.3 | 拖到底再回退 | 页码能正确回到 `N-1`，不越界 | | |
| C2.4 | 点上一页到第 1 页 | 按钮禁用，页码停在 1 | | |

### C3 · 退回单栏（响应式）

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C3.1 | 在双栏态把窗口**高度**拖到 < 820 | 正文变单栏**纵向滚动**，pager 消失，出现提示「窗口高 < 820px 或正文宽 < 760px，按原型退回单栏滚动。」 | | |
| C3.2 | 把窗口**宽度**拖到正文内容宽 < 760 | 同上 | | |
| C3.3 | 拖回双栏条件 | 恢复两栏，且**阅读位置不丢** | | |
| C3.4 | 在退回态按 `→` / `←` | 不应被拦截，正常滚动 | | |

### C4 · 精确分页：改字号不丢进度

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C4.1 | 双栏态翻到第 3 页，记住首行文字 | —— | | |
| C4.2 | 打开右侧面板 → 阅读样式 → 字号 17 → 22 | 页数 N 变大；**视觉位置停在同一段文字附近（允许 ±1 行）**，不能跳回开头 | | |
| C4.3 | 继续改 行距 / 段距 / 页边距 | 同上，每次都不跳 | | |
| C4.4 | 改完看 pager | 页码与「本章 %」自洽（百分比不应倒退到 0） | | |

### C5 · 面板与目录挤压重排

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C5.1 | 双栏态打开右侧面板（320px） | 页数重算（通常变多），**当前页不跳回第 1 页** | | |
| C5.2 | 再关闭面板 | 页数回落，位置保持 | | |
| C5.3 | 打开 / 关闭目录侧栏（288px） | 同上；目录关闭时正文应变宽 | | |

### C6 · 目录状态与分组

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C6.1 | 读到第 3 章后打开目录 | 第 1、2 章右侧显示「已读」；当前章显示 `xx%` | | |
| C6.2 | 观察「已读」的判定边界 | 以**上次保存的阅读位置**为界（`lastReadChapterId`）；当前章不显示「已读」而是百分比 | | |
| C6.3 | 用 D5 那本书 | 有「第X卷/部/篇」前缀的章节分到对应卷标题下 | | |
| C6.4 | 目录搜索框输入关键词 | 只留匹配章节；清空后恢复 | | |
| C6.5 | 点当前章 | 高亮正确（`is-current` 等效样式） | | |

### C7 · 正文排版细节

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C7.1 | 用 D6 那本书 | 以 `"` `“` `「` `『` `—` 开头的段落左缩进 2em 且**无首行缩进** | | |
| C7.2 | 观察普通段落 | 首行缩进 2em | | |
| C7.3 | 切换四种背景（浅色/护眼/深色/黑夜） | 文字与背景对比度都够；分栏竖线可见 | | |
| C7.4 | 调亮度 + 开护眼 | 亮度生效；护眼叠加 sepia | | |

### C8 · 沉浸模式与其余控件

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C8.1 | 按 `F` | 顶栏与底栏 **height→0 收起**（内容区平滑扩展，不是瞬间消失）；再按恢复 | | |
| C8.2 | 滚动模式读到中间 → 切分页 | 位置保持（不回到开头） | | |
| C8.3 | 加书签 → 切下一章 → 切回 | 书签仍在，底部按钮高亮；目录「书签」页可跳转 | | |
| C8.4 | 打开自动翻页 | 约 12 秒后自动翻一页/滚一屏；再点关闭后停止 | | |
| C8.5 | 正文搜索输入关键词 | 显示命中数，滚动模式跳转到首个命中 | | |
| C8.6 | 点底部 听书 / 翻译 / AI 总结 / 文本处理 / 更多 | 弹出「未接入」并**列出所需能力**；不出现任何假数据 | | |
| C8.7 | 底部 12 个工具按钮 + 可拖动章节轨道 | 轨道拖动跳章正确，百分比与「第 N/M 章」同步 | | |

### C9 · 阅读时长与统计联动

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C9.1 | 在阅读器停留 ≥ 40 秒（保持窗口可见） | 每 15 秒上报一次；切到首页看「今日分钟」增长 | | |
| C9.2 | 把窗口最小化/切到别的应用停留 40 秒 | **不应**计时（`document.visibilityState` 检查） | | |
| C9.3 | 关闭阅读器再重新打开 | 进度恢复到上次位置 | | |

### C10 · 预下载附近章节（对位 `ReadBook.preDownload()`）

前置：`#/settings/cache` 里把「预下载章节数」保持在默认 10；用一本**真实在线书**（见 D4）。
缓存章数以设置页「正文缓存」的「N 章」为准，日志看 `%APPDATA%\com.reader.desktop\logs\reader-desktop.log`
（`target: reader`）。**自动化的部分只有窗口规则**（`cargo test prefetch`，11 条），下面全是真机项。

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| C10.1 | 打开第 10 章，记下打开瞬间的缓存章数，静置 10 秒 | 缓存章数按「向前 10 + 向后至多 5」增长；日志出现 `chapter prefetch finished`（`planned` 与实际新增一致） | | |
| C10.2 | 打开第 1 章 | 向前不越界（目录头即停）；向后为空，不报错 | | |
| C10.3 | 打开最后一章 | 向前为空；向后至多 5 章 | | |
| C10.4 | 连续快速点「下一章」8 次 | 只有最后一次窗口继续（旧窗口日志 `chapter prefetch cancelled`）；同一域名同时最多 2 个请求，不出现卡顿 | | |
| C10.5 | 把「预下载章节数」改成 0 → 保存 → 换一章 | 缓存章数不再增长（`chapter_contents` 无新行） | | |
| C10.6 | 改回 10 → 打开一个从未读过的章节两次（第二次应为缓存命中） | 第二次进入该章无网络等待；日志 `chapter cache hit` | | |
| C10.7 | 打开本地 TXT 书并翻章 | 完全无预取（本地书无书源），日志无 `chapter prefetch` | | |
| C10.8 | 预取进行中点顶栏关闭按钮 | 日志出现 `chapter prefetch cancelled`，缓存章数停止增长 | | |

> 一键核对缓存增量（装过 `sqlite3` 才可用；`<user>` 换成你的账户名）：
> ```powershell
> sqlite3 "$env:APPDATA\com.reader.desktop\app.db" "SELECT COUNT(*) FROM chapter_contents;"
> ```

---

## D. F3 性能与端到端验收（v2 遗留项）

| # | 步骤 | 期望 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| D1 | 导入 D1 的百万字 TXT，打开第一章并计时 | 正文出现 **< 1s**；上下滚动无明显掉帧（目测，必要时用任务管理器看 CPU） | | |
| D2 | 用 D4 那本真实在线书，从第 1 章读到末章 | 全程不崩溃、不卡死；章节切换正常；读完后「累计读完」+1 | | |
| D3 | 直接访问 `#/read/<bookId>?toc=0&panel=1` | 目录关闭、右侧面板打开；`toc=0` 生效（原型 :2778–2794） | | |
| D4 | 直接访问 `#/settings/backup`、`#/settings/lab` | 命中对应 pane（深链接） | | |
| D5 | 重启应用后回到 `#/read/<bookId>` | 恢复到上次章节与位置 | | |
| D6 | 双击 `src-tauri/target/release/reader-desktop.exe`（**不要**从终端启动） | **不出现任何控制台窗口**；日志写在 `%APPDATA%\com.reader.desktop\logs\reader-desktop.log` | | |
| D7 | 同上，用 `cargo tauri build` 出的安装包安装后再启动 | 同样无控制台窗口；卸载后 `%APPDATA%\com.reader.desktop` 是否保留由你决定 | | |

> D6/D7 的自动核对（PE subsystem 必须为 2 = GUI）：
> ```powershell
> $p='src-tauri\target\release\reader-desktop.exe'
> $h=Get-Content $p -AsByteStream -TotalCount 512
> # 注意：byte 上的 -shl 会按 byte 溢出，必须先 [int]
> $e=[int]$h[0x3C]+([int]$h[0x3D]-shl 8)+([int]$h[0x3E]-shl 16)+([int]$h[0x3F]-shl 24)
> [int]$h[$e+24+68]+([int]$h[$e+24+69]-shl 8)   # 期望 2；3 表示控制台程序
> ```

---

## E. 纪律 F0 逐条核查（§6.1 #4）

逐个打开下列位置，确认**只出现未接入态，不出现其它模块的数据**：

| # | 位置 | 应显示 | 实测 | 结论 |
| ---: | --- | --- | --- | --- |
| E1 | 首页 · 侧栏 WebDAV 卡 | 未接入 · WebDAV 同步 | | |
| E2 | 首页 · 空书架时的「最近在读」 | 未接入（提示导入一本书） | | |
| E3 | RSS 整页 | 未接入 · RSS 订阅 | | |
| E4 | 历史整页 | 未接入 · 阅读记录 | | |
| E5 | 书签整页 | 未接入 · 书签列表 | | |
| E6 | 阅读面板 · 听书 / AI 两个 tab | 未接入（各列所需能力） | | |
| E7 | 设置 · 封面 / AI / 翻译 / 实验室 | 未接入 | | |
| E8 | 设置 · 备份恢复 里的 legado ZIP/AES | 未接入 | | |
| E9 | 我的 · Web 服务 / AI 对话 / 听书与翻译 | 未接入 | | |
| E10 | 全局抽查 | 未接入态里**没有**任何书名、章节名、阅读时长等真实业务数据 | | |

> 反例（历史上真发生过）：`LibraryTabPage.vue` 曾在「RSS 订阅」标题下渲染书架里的书。

---

## F. 门槛汇总（回填用）

| # | 门槛 | 证明方式 | 本次由谁证明 | 结论 |
| ---: | --- | --- | --- | --- |
| 1 | 无 `.vue` > 200 行 | `Get-ChildItem -Recurse src -Filter *.vue \| ...` 最大 189 | 自动（已过） | ✅ |
| 2 | 路由不再全指向 AppShell | `grep -c 'component: AppShell' src/router/index.ts` = 0 | 自动（已过） | ✅ |
| 3 | 无死代码导出 | `features/*/index.ts` 已删 | 自动（已过） | ✅ |
| 4 | 无似是而非的数据 | §E | **手测** | |
| 5 | 令牌化完成 | `src/styles.css` 硬编码十六进制 = 0 | 自动（已过） | ✅ |
| 6 | 手写 CSS 收缩 | `styles.css` 424 行，全部为令牌 + 阅读器排版层 | 自动（已过） | ✅ |
| 7 | 七页与原型对位 | §B 逐页截图 | **手测** | |
| 8 | 九组快捷键可用 | §A | **手测** | |

---

## G. 缺陷登记模板

```markdown
### [C4.2] 改字号后阅读位置跳回章首
- 环境：Windows 11 / WebView2 1xx / 窗口 1280×900 / rail 收起 / 目录关
- 复现：1. 分页双栏翻到第 3 页 2. 面板改字号 17→22
- 实测：跳回第 1 页，pager 显示「第 1 / 9 页」
- 期望：停在原段落附近（±1 行）
- 证据：docs/qa/20260910/C4.2.png
- 初步定位：`src/features/reader/useReader.ts` 的字体 watcher → `captureReadingLocator`
- 归属：F3 剩余 / E0 / E1 / S（择一）
```

登记后写进 `ROADMAP-v3.md` §8 风险登记，**不要**把对应条目的 `[ ]` 改成 `[x]`。

---

## H. 通过后如何回填

1. 全部 ✅ → 把 `ROADMAP-v3.md` 中：
   - `F1 · 壳层与导航` 验收「九组快捷键逐一手测通过」
   - `F2` 验收「每页与 `desktop-ui.html` 对应 section 并排截图比对」
   - `F3` 验收三条（百万字 < 1s / 改字号不丢进度 / 读完一本在线书）
   的 `[ ]` 改为 `[x]`，并在每处补「实测日期 + 执行人 + 截图目录」。
2. §6.1 表格的 #4 / #7 / #8 行补结论。
3. 有 ❌ → 只更新 §8 风险登记与对应缺陷，勾保持原样。
4. `v0.8.0` / `v0.9.0` 的状态只在**该版本全部门槛**通过后才改。
